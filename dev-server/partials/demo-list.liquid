{% if isIndex and demoCount > 0 %}
<nav class="demo-navigation">
  <h2>Available Demos ({{ demoCount }})</h2>
  <ul class="demo-list">
    {% for demo in demos %}
    <li>
      <a href="{{ demo.url }}">{{ demo.title }}</a>
    </li>
    {% endfor %}
  </ul>
</nav>
{% endif %}

